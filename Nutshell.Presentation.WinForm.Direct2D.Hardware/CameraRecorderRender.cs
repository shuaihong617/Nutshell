using System.Windows.Forms;
using Nutshell.Presentation.Direct2D1;
using SharpDX;
using SharpDX.Direct2D1;

namespace FutureTech.Windows.Forms.Direct2D.Hardware
{
        //public class CameraRecorderRender :Bgra32CameraRender
        //{
        //        public CameraRecorderRender(string key, Control control, CameraRecorder cameraRecorder)
        //                : base(key, control, cameraRecorder.Camera)
        //        {
        //                CameraRecorder = cameraRecorder;
        //        }

        //        public CameraRecorder CameraRecorder { get; private set; }

        //        protected override void Draw(BitmapRenderTarget target)
        //        {
        //                base.Draw(target);


        //                Brush.Color = CameraRecorder.IsEnable ? Colors.Green : Colors.Red;
        //                target.DrawText(CameraRecorder.IsEnable ? "录制" : "停录", YaHei40TextFormat,
        //                        new RectangleF(Width - 220, 20, 100, 50), Brush);
        //        }
        //}
}