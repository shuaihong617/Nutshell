namespace Nutshell.Automation.IOBoard
{
        public abstract class InputChannel : Channel
        {
                protected InputChannel(int index)
                        : base(index)
                {
                }
        }
}
